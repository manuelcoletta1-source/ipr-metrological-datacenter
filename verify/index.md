# Verify — IPR-Datacenter

Questa sezione descrive la verifica pubblica di:
- IPR Manifest (hash-only)
- Decision Gate output (PASS/FAIL + reason_code)
- Evidence Pack (hash-only)

Regola:
in assenza di prova verificabile → nessuna esecuzione (fail-closed).
